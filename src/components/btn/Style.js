import styled from "styled-components";

export const Button = styled.button`
    padding: 10px 20px;
    background-color: #fff;
    border-radius: 10px;
    border: none;
    margin-left: 20px;
    font-size: 22px;
    font-weight: 500;
    cursor: pointer;
    transition: 0.5s;
    /* display: block; */

    &:hover {
        color: blue;
    }
`